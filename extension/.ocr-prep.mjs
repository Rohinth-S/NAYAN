import sharp from 'sharp'; import { createWorker, PSM } from 'tesseract.js'; import {readFile} from 'node:fs/promises';
for(const [src,out] of [['../.runtime/credit.png','../.runtime/credit-prep.png'],['../.runtime/aadhaar.png','../.runtime/aadhaar-prep.png']]) await sharp(src).resize({width:1000,withoutEnlargement:false}).grayscale().linear(1.8,-102.4).png().toFile(out);
const w=await createWorker(['eng'],1,{langPath:'./models/ocr/lang-data',gzip:false,cacheMethod:'none',logger:()=>{},errorHandler:()=>{}}); await w.setParameters({tessedit_pageseg_mode:PSM.SINGLE_BLOCK,preserve_interword_spaces:'1',user_defined_dpi:'220'});
for(const f of ['../.runtime/credit-prep.png','../.runtime/aadhaar-prep.png']){const r=await w.recognize(await readFile(f),{}, {blocks:true}); console.log('\nFILE',f,'\n'+r.data.text); for(const b of r.data.blocks??[]) for(const p of b.paragraphs??[]) for(const l of p.lines??[]) console.log(JSON.stringify(l.text),l.confidence); } await w.terminate();
