import sharp from 'sharp'; const source='../cdp-shot.png'; const ex={left:649,top:254,width:546,height:333}; for (const [name,p] of [['gray',sharp(source).extract(ex).grayscale().normalize().resize(1092,666,{kernel:'lanczos3'})],['contrast',sharp(source).extract(ex).grayscale().linear(1.8,-100).resize(1092,666,{kernel:'lanczos3'})],['sharpen',sharp(source).extract(ex).grayscale().sharpen({sigma:1.2}).resize(1092,666,{kernel:'lanczos3'})]]) await p.png().toFile('../crop-'+name+'.png');
